"""Runnable, copy-pasteable examples for the anet.svc SDK.

Run any of them after `anet daemon` is up:

    python -m anet.examples.01_register_local_service
    python -m anet.examples.02_discover_and_call
    python -m anet.examples.03_stream_consume

Each example is fully self-contained and < 100 lines.
"""
